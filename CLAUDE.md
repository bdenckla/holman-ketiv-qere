# CLAUDE.md

holman-ketiv-qere is a redirect host. Its data and Python live in
`C:/Users/BenDe/GitRepos/MAM-basics`; this repository has no Python and no data.

`gh-pages/` is generated redirect stubs. Do not hand-edit it. From the
MAM-basics repository, regenerate the stubs with:

```powershell
C:/Users/BenDe/GitRepos/MAM-basics/.venv/Scripts/python.exe py/main_redirect_stubs.py build --repo holman-ketiv-qere --publish
```

A bare `#NN` in this repository means an issue in
`bdenckla/holman-ketiv-qere`.
