The user's argument is a string containing HTML dimension attributes like `width="209" height="259"`. Halve each numeric value and return the result as a single line of text. Round halves to the nearest integer.

Example input: `width="209" height="259"`
Example output: `width="105" height="130"`

Output the resulting string inside a fenced code block (for the copy button) — nothing else.